(() => {
  const { pageHelper } = ShopbySkin;

  const termDetailLayerModalHelper = pageHelper.termDetailLayerModalHelper();

  termDetailLayerModalHelper.initialize({
    layerModalHelperKey: 'term-detail',
  });
})();
