(() => {
  const { pageHelper } = ShopbySkin;
  const findIdResultLayerModalHelper = pageHelper.findIdResultLayerModalHelper();

  findIdResultLayerModalHelper.initialize({
    layerModalHelperKey: 'find-id-result',
  });
})();
