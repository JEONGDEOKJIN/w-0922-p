(() => {
  const { pageHelper } = ShopbySkin;

  const kcpSmsAuthenticationLayerModalHelper = pageHelper.kcpSmsAuthenticationLayerModalHelper();

  kcpSmsAuthenticationLayerModalHelper.initialize({
    layerModalHelperKey: 'kcp-sms-authentication',
  });
})();
