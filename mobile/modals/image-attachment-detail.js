(() => {
  const { pageHelper } = ShopbySkin;
  const imageAttachmentDetailLayerModalHelper = pageHelper.imageAttachmentDetailLayerModalHelper();

  imageAttachmentDetailLayerModalHelper.initialize({
    layerModalHelperKey: 'image-attachment-detail',
  });
})();
